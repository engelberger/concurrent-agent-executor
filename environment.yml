name: concurrent_agent_executor
channels:
  - defaults
  - conda-forge
dependencies:
  - pip:
      - chainlit==0.6.0
      - textual
      - aioconsole

      - openai
      - pyee
      - langchain==0.0.231
      - human-id
      - colorama
      - pydantic
      - python-dotenv
  - pip
